const axios = require("axios");

async function testApi() {
  try {
    // First we need a token or we can just capture the stack trace inside the app
    // Actually it's easier to modify the controller to log to a file.
  } catch (err) {
    console.error(err);
  }
}
